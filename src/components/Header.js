import React from "react";
import MainMenu from '../common/components/MainMenu';

const Header = (props) => {
    
    return (
      <div>
        <MainMenu/>
      </div>
    );
}

export default Header;
